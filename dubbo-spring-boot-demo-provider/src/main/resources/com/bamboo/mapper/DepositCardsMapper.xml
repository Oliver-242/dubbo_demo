<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bamboo.mapper.DepositCardsMapper">
    <select id="selectMoneyByCardId" parameterType="java.lang.String" resultType="java.lang.Long">
        select money from depositcards where cardid=#{cardId}
    </select>

    <update id="depositMoneyByCardId">
        update depositcards set money=money+#{arg1} where cardid=#{arg0}
    </update>

    <update id="withdrawMoneyByCardId">
        update depositcards set money=money-#{arg1} where cardid=#{arg0}
    </update>

    <update id="transferMoney">
        update depositcards set money=money+#{arg2} where cardid=#{arg1};
        update depositcards set money=money-#{arg2} where cardid=#{arg0};
    </update>

</mapper>